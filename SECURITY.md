# Security

## API keys

Never commit a Gemini API key. Use `.env`, the application password field, or deployment secrets. If a key is exposed in Git history, revoke it immediately and create a replacement; removing the visible text from a later commit is not sufficient.

## Reporting a vulnerability

Please report security issues privately to the repository owner rather than opening a public issue containing exploit details or credentials.
