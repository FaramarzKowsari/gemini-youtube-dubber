v0.2.4 — Background Execution + Checkpoint Auto Patch

1. Extract this ZIP DIRECTLY into:
   C:\Faramarz\GitHub\gemini-youtube-dubber-github

2. Double-click:
   APPLY_V024_PATCH_WINDOWS.bat

3. When it says "patch applied successfully", open GitHub Desktop.

4. Summary:
   Use Gemini background execution and checkpoints (v0.2.4)

5. Commit to main
6. Push origin
7. Start a BRAND-NEW Actions -> Cloud Dub -> Run workflow.

Do not re-run old cancelled/failed runs.

What changes:
- Video analysis uses Gemini Interactions background=True.
- It polls the Interaction ID every 5 seconds.
- Poll network resets reconnect to the SAME server-side job.
- Cloud analysis is capped at 240 seconds per model, then falls back.
- transcript.json is saved immediately after successful analysis.
- If TTS later fails, process attempt #2 reuses that checkpoint.
- Whole-process attempts are reduced from 3 to 2.
- Cloud job timeout is reduced to 45 minutes.
